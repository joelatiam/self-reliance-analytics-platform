version: 2

models:
  - name: mart_country_indicators
    columns:
      - name: country_code
        tests: [not_null]
      - name: indicator_code
        tests: [not_null]
      - name: year
        tests: [not_null]
      - name: value
        tests: [not_null]

  - name: mart_indicator_yoy_growth
    columns:
      - name: country_code
        tests: [not_null]
      - name: year
        tests: [not_null]
